apiVersion: v1
kind: ConfigMap
metadata:
  name: security-operator-config
data:
  scan-interval: "5m"
  scanners: "rbac,network,orphaned,securitycontext"
  log-level: "info"
  log-format: "json"
  backend-url: "http://localhost:8080" 
