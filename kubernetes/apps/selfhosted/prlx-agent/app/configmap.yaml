---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prlx-agent-config
data:
  config.json: |
    {
      "scan_interval": "5m",
      "output_path": "/var/run/prlx-agent/reports",
      "retention_days": 7
    }
