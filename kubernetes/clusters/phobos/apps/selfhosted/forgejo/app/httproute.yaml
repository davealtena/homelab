---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: forgejo
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - "git.${CLUSTER_DOMAIN}"
  rules:
    - backendRefs:
        - name: forgejo-http
          port: 3000
